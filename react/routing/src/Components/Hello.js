import React from 'react';
import { Router } from '@reach/router';

const Hello = (props) =>{
    return (
        <div>
            <h1>(props.hello)</h1>
        </div>
    )
}

export default Hello;
       
    
